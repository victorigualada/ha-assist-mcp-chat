{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "Connect to ha-mcp",
        "description": "Enter your ha-mcp server URL. On Home Assistant OS / Supervised the add-on URL is prefilled — just paste the secret path from the add-on log.{addon_hint}",
        "data": {
          "url": "URL",
          "secret_path": "Secret path"
        },
        "data_description": {
          "url": "Base URL of the ha-mcp server, for example {example_url}.",
          "secret_path": "The secret path from the add-on's MCP URL, for example {example_path}. Leave empty if the URL already includes it."
        }
      }
    },
    "error": {
      "invalid_url": "The URL is not valid.",
      "cannot_connect": "Failed to connect to the ha-mcp server.",
      "timeout_connect": "Timed out while connecting to the ha-mcp server.",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This ha-mcp server is already configured.",
      "missing_capabilities": "The server does not expose any tools, so it cannot be used."
    }
  }
}
